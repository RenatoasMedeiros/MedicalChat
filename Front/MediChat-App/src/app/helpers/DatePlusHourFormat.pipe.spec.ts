/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DatePlusHourFormatPipe } from './DatePlusHourFormat.pipe';

describe('Pipe: DatePlusHourFormate', () => {
  it('create an instance', () => {
    let pipe = new DatePlusHourFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
